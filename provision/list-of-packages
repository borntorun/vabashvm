update-yum.sh		- to update the guest system

add-user.sh			- to add a user to the system

nodejs-nvm.sh		- install Node.js with Node Version Manager

nodejs-redhat-global.sh		- install Node.js global via packager manager

heroku.sh			- install heroku toolbelt

git.sh				- install git (chossing version in args ou install latest)

net-eth-rules.sh	- configure network interfaces to use eth* name rules (CentOS 7 systems) 

hostname.sh			- set hostname on guest system

mongodb.sh		- install MongoDB 32 or 64 (default) bit version
